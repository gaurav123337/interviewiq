import type { Field } from "../types";

/* Frontend, Backend, Full-Stack, DevOps — 5 levels × 4-5 questions each with model answers */

export const FIELDS_PART1: Field[] = [
  {
    id: "frontend", name: "Frontend Engineer", icon: "🖥️",
    blurb: "UI, UX, browsers, performance and accessibility.",
    skills: ["JavaScript / TypeScript", "React · Vue · Angular", "CSS & accessibility", "Web performance"],
    questions: {
      junior: [
        { q: "Explain the difference between `let`, `const`, and `var` in JavaScript.",
          a: "`var` is function-scoped and hoisted (initialized as undefined), while `let` and `const` are block-scoped and live in the temporal dead zone until declared. `let` allows reassignment, `const` does not — though `const` objects and arrays can still be mutated. Modern code prefers `const` by default, `let` only when you must reassign, and avoids `var` entirely.",
          kp: ["block scoping", "const cannot be reassigned", "hoisting", "temporal dead zone", "prefer const by default"] },
        { q: "What is the DOM and how does JavaScript interact with it?",
          a: "The Document Object Model is the browser's tree-structured representation of the HTML page. JavaScript interacts with it through APIs like `document.querySelector`, `getElementById`, `createElement` and `addEventListener` to read, modify, and respond to the page. Every change can trigger layout and paint, so you should batch updates and minimize direct DOM manipulation.",
          kp: ["tree representation of HTML", "querySelector and getElementById", "addEventListener", "creating and appending nodes", "minimize reflows and repaints"] },
        { q: "Describe the CSS box model and how `box-sizing` affects it.",
          a: "Every element is a box made of content, padding, border, and margin. By default (content-box), `width` sets only the content width, so padding and border add to the rendered size. With `box-sizing: border-box`, the width includes padding and border, which makes layouts far more predictable — it's the standard reset used in modern codebases.",
          kp: ["content padding border margin", "border-box includes padding and border", "margin collapsing", "predictable sizing"] },
        { q: "What's the difference between `==` and `===` in JavaScript?",
          a: "`==` performs type coercion before comparing, which leads to surprising results like `0 == false` being true. `===` (strict equality) compares both value and type without coercion. You should always prefer `===`, and use explicit checks like `x == null` only when you deliberately want to catch both `null` and `undefined`.",
          kp: ["loose vs strict equality", "type coercion", "prefer triple equals", "null and undefined edge cases"] },
        { q: "What is a Promise in JavaScript and why was it introduced?",
          a: "A Promise represents a value that may not be available yet — it's a cleaner way to handle asynchronous work than nested callbacks, avoiding 'callback hell'. A promise has three states: pending, fulfilled, and rejected, and you chain with `.then()` / `.catch()` or `async`/`await`. It also gives you a single error path and composition tools like `Promise.all`.",
          kp: ["pending fulfilled rejected", "avoids callback hell", "then and catch chaining", "async await", "Promise.all"] }
      ],
      mid: [
        { q: "Explain closures in JavaScript and give a real-world use case.",
          a: "A closure is a function that retains access to variables from its outer scope even after that scope has returned. Common uses: data privacy (module pattern), creating function factories or counters, and capturing values in event handlers and `setTimeout` callbacks — which is exactly why `var` in a loop historically caused bugs. Closures keep references alive, so misuse can cause memory leaks.",
          kp: ["inner function remembers outer scope", "data privacy and module pattern", "capturing loop variables", "memory leaks", "function factories"] },
        { q: "Explain the event loop. How do microtasks and macrotasks differ?",
          a: "JavaScript is single-threaded: the call stack runs one task at a time, and asynchronous callbacks are queued. The event loop continuously checks the call stack, then the microtask queue, then the macrotask queue. Microtasks (promise callbacks, `queueMicrotask`) always drain before the next macrotask (`setTimeout`, I/O), which is why promise callbacks can starve timers if overused.",
          kp: ["single-threaded call stack", "microtasks before macrotasks", "promises are microtasks", "setTimeout is a macrotask", "rendering happens between tasks"] },
        { q: "Explain React's virtual DOM and reconciliation.",
          a: "React keeps an in-memory representation of the UI and differs it against the previous render to compute the minimal set of DOM updates — that's reconciliation. Keys tell React which list items are stable across renders so it can reuse DOM nodes instead of rebuilding them. Updates are batched, and the commit phase applies the changes; misuse of keys or unstable components forces full re-renders and kills performance.",
          kp: ["in-memory UI tree", "diffing algorithm", "keys for list identity", "batched updates", "minimal DOM mutations"] },
        { q: "How do you optimize a slow web page? Walk me through your approach.",
          a: "First I measure — Lighthouse, DevTools performance panel, and real-user metrics — because guesses are usually wrong. Then I attack the biggest wins: reduce bundle size with code splitting and tree-shaking, lazy-load below-the-fold content, optimize images (modern formats, sizing, lazy loading), and reduce main-thread work like unnecessary re-renders. Finally I verify with before/after metrics and add budgets so regressions get caught in CI.",
          kp: ["measure before optimizing", "code splitting and lazy loading", "image optimization", "reduce re-renders", "performance budgets in CI", "real-user monitoring"] },
        { q: "How would you debug a memory leak in a single-page application?",
          a: "I'd start in the DevTools Memory panel: take heap snapshots before and after actions, look for detached DOM nodes and listeners still referenced by global variables or closures. Common culprits are unremoved event listeners, `setInterval` never cleared, and closures capturing large objects. I'd reproduce a leak in the Performance monitor, isolate the component, and verify the fix by repeating the snapshot comparison.",
          kp: ["heap snapshots", "detached DOM nodes", "unremoved event listeners", "intervals and timers not cleared", "closures capturing large objects"] }
      ],
      senior: [
        { q: "Design a state management approach for a large React application.",
          a: "I'd start with the principle 'as local as possible': colocate state with the component that owns it, lift only what's genuinely shared. Server data goes through a data-fetching layer (like React Query or SWR) with cache keys and invalidation, while a lightweight store or context handles truly global client state like auth or theme. Derived values are computed with selectors or memoization rather than duplicated in state, and updates flow through a predictable, testable path.",
          kp: ["colocate state locally", "separate server and client state", "cache invalidation", "selectors for derived state", "avoid prop drilling", "predictable update flow"] },
        { q: "How do you make an application accessible, and how do you verify it?",
          a: "Accessibility starts with semantic HTML — real buttons, landmarks, and headings — which buys most of the a11y for free. ARIA is used only to fill gaps, never to paper over bad markup. I verify with automated tools (axe in CI), keyboard-only navigation tests, contrast checks, and real screen-reader passes (VoiceOver/NVDA) on key flows. Accessibility issues are tracked like bugs, not afterthoughts.",
          kp: ["semantic HTML first", "ARIA only when needed", "keyboard navigation", "color contrast", "screen reader testing", "automated axe checks in CI"] },
        { q: "How would you migrate a legacy monolith frontend to a modern stack without stopping delivery?",
          a: "I'd use the strangler pattern: carve the app into bounded pieces and migrate one at a time behind a router so old and new live side by side. Feature flags gate each slice, and a contract-first approach keeps APIs stable during transition. For cross-app embedding I'd consider iframes or web components for isolation, and micro-frontends only when team autonomy demands it — they add real complexity. The key rule: the codebase stays deployable at every step.",
          kp: ["strangler pattern incremental migration", "feature flags", "contract-first APIs", "web components for isolation", "deployable at every step", "avoid micro-frontends unless justified"] },
        { q: "How do you ensure frontend performance at scale — specifically Core Web Vitals?",
          a: "Core Web Vitals (LCP, CLS, INP) have to be owned like product metrics, not fixed reactively. LCP: minimize server response, deliver critical CSS and preload key resources, use next-gen images. CLS: reserve space for images/ads, avoid injecting layout above the fold. INP: keep the main thread free — avoid long tasks, lazy hydrate, debounce expensive work. I'd instrument with real-user monitoring, set budgets in CI, and tie regressions back to the commit that caused them.",
          kp: ["LCP CLS INP definitions", "critical CSS and font loading", "next-gen images and preloading", "long tasks and main-thread time", "real-user monitoring", "budgets enforced in CI"] },
        { q: "How do you handle state and navigation in an app with dozens of routes and deep linking?",
          a: "I'd separate route definitions from components, keep URL as the source of truth for shareable state, and derive what's ephemeral (scroll positions, in-flight form state) from memory. Code-split per route, lazy-load heavy screens, and define a small set of navigation transitions. Deep links map to route params, with guards handling auth and not-found states centrally.",
          kp: ["URL as source of truth", "route-based code splitting", "lazy loading screens", "route guards for auth", "deep link to route mapping"] }
      ],
      staff: [
        { q: "Design a frontend platform team's offering to 10+ product teams.",
          a: "The platform should make the right thing easy: a versioned design system and component library, shared build tooling with CI templates, and a CLI/scaffolder so a new app starts with best practices baked in. Documentation, migration guides, and a support model (office hours, champions per team) matter as much as the code. The hard part is governance: the platform sets standards, but product teams keep ownership of their code, so you need a lightweight way to approve exceptions.",
          kp: ["versioned design system", "shared build tooling and CI templates", "scaffolding and CLI", "documentation and support", "champions in each team", "flexibility vs consistency balance"] },
        { q: "Many teams ship daily. How do you prevent regressions across the frontend?",
          a: "Combine layers of automated safety: type checks and contract tests at merge time, visual regression testing on key flows, automated a11y scans, and canary/staged rollouts that watch error and performance signals before full release. Feature flags let you turn off a bad change instantly. The last layer is ownership: every shared component has an owning team, and regression monitoring is instrumented so problems surface in minutes, not weeks.",
          kp: ["visual regression testing", "contract and type checks", "canary and staged rollouts", "feature flags as kill switch", "ownership of shared code", "automated monitoring"] },
        { q: "Your performance budget is being blown by 20 teams. What do you do?",
          a: "First, instrument and rank — most of the damage usually comes from a few offenders, so I'd measure per-team bundle and runtime impact and publish the numbers. Then I'd make compliance cheap: central rules in the bundler, shared image/CDN infrastructure, and a hard budget gate in CI with a clear exception process. Culture follows structure: celebrate the teams that hit budgets, and tie a senior champion to each top offender rather than chasing all 20 at once.",
          kp: ["measure and rank offenders", "central bundler rules", "shared infrastructure", "budget gates in CI", "champions and incentives", "celebrate measurable wins"] },
        { q: "Compare micro-frontends, module federation, and a monolith for a 5,000-engineer company.",
          a: "A monolith is usually right: it keeps integration cheap, one version of everything, and shared performance tooling. Module federation adds runtime module sharing with real costs — version skew, dependency conflicts, and debugging across boundaries. Micro-frontends buy team autonomy and independent deploys, but you pay in integration testing, duplicated dependencies, and fragmented UX. My answer: start monolith, adopt federation only where independent deploy velocity is proven to matter, and never fragment for architectural fashion.",
          kp: ["monolith as default", "module federation version skew", "micro-frontends integration cost", "team autonomy tradeoff", "incremental adoption", "shared performance tooling"] }
      ],
      principal: [
        { q: "Define the frontend technical strategy for the next 3 years.",
          a: "It must start from business goals: where does the company need speed, reliability, or new surfaces? I'd make a small number of explicit bets — a consolidated framework and rendering strategy, a design system roadmap, performance as a product KPI — and kill fragmentation everywhere else. The plan includes a hiring and training path, a migration roadmap with measurable milestones, and a quarterly review cadence with the exec team so the strategy stays alive instead of collecting dust.",
          kp: ["aligned to business goals", "consolidated framework bet", "performance as product", "migration roadmap", "training and hiring plan", "measurable KPIs and review cadence"] },
        { q: "How do you drive adoption of a new architecture across an org that resists change?",
          a: "Resistance usually means the change isn't obviously better for the people doing it. I'd find 1-2 respected early-adopter teams, build the new path *with* them rather than for them, and let their measurable wins (faster deploys, fewer bugs) sell it. Executive sponsorship sets the direction, but persuasion comes from evidence. I'd make the migration path low-friction with tooling and migration scripts, keep exceptions visible, and publicly celebrate every team that converts.",
          kp: ["early adopter teams first", "build with not for", "measurable wins as evidence", "executive sponsorship", "low-friction migration tooling", "celebrate and document success"] },
        { q: "You must cut frontend infrastructure costs by 40% without losing reliability.",
          a: "Start with the data: tag all resources, find what's idle or duplicated, and kill waste before touching anything that works. Then structural wins: move static assets to a cheaper CDN tier with smarter caching, reduce bundle and image weight to cut bandwidth, and right-size build runners. Where possible, shift bursty compute to serverless. Finally, put cost guardrails in place — budgets and alerts — so the 40% doesn't creep back, and communicate every tradeoff in terms of what didn't change (reliability, UX).",
          kp: ["audit usage before cutting", "tagging and waste elimination", "CDN and caching strategy", "bundle and image weight", "serverless for bursty work", "guardrails and cost monitoring"] },
        { q: "How do you set the hiring bar and interview process for frontend engineers org-wide?",
          a: "A credible process starts with a rubric: what does a frontend engineer at each level actually do here? Interviews become structured around those dimensions — coding, system design, debugging, collaboration — with consistent questions and a calibration loop where interviewers compare scores and share signal. I'd invest in the candidate experience (clear expectations, fast feedback) because strong candidates are evaluating us too, and track outcomes — hire quality and performance — to keep improving the process itself.",
          kp: ["role-based rubrics per level", "structured consistent interviews", "calibration across interviewers", "candidate experience", "track hire quality", "continuous process improvement"] }
      ]
    }
  },
  {
    id: "backend", name: "Backend Engineer", icon: "⚙️",
    blurb: "APIs, databases, distributed systems and reliability.",
    skills: ["APIs & services", "Databases & caching", "Distributed systems", "Go · Java · Node · Python"],
    questions: {
      junior: [
        { q: "Walk me through what happens when you type a URL into a browser and press Enter.",
          a: "The browser resolves the domain via DNS to an IP, opens a TCP connection (TLS handshake for HTTPS), and sends an HTTP request. The server processes it, typically hitting a load balancer, application code, and possibly a database, then returns an HTTP response with status code and body. The browser parses the response, and caching layers (browser, CDN) may shortcut the whole flow on repeat visits.",
          kp: ["DNS resolution", "TCP and TLS handshake", "HTTP request and response", "server processing and database", "status codes", "caching layers"] },
        { q: "Explain REST and common HTTP methods and status codes.",
          a: "REST treats resources identified by URLs, manipulated with HTTP verbs: GET to read, POST to create, PUT to replace, PATCH to partially update, DELETE to remove. Status codes group outcomes: 2xx success, 3xx redirects, 4xx client errors (404 not found, 400 bad request, 401/403 auth), 5xx server errors. REST is stateless — each request carries what it needs — and PUT/DELETE are idempotent, meaning repeating them has the same effect.",
          kp: ["resources identified by URL", "GET POST PUT DELETE semantics", "2xx 4xx 5xx status codes", "statelessness", "idempotency of PUT DELETE"] },
        { q: "What's the difference between an INNER JOIN and a LEFT JOIN in SQL?",
          a: "Both combine rows from two tables on a condition. INNER JOIN returns only rows that match in both tables; unmatched rows disappear. LEFT JOIN returns every row from the left table, padding the right side with NULLs where there's no match. LEFT JOINs are common for 'all X, with their Y if any' queries, but watch for row multiplication when the joined side has duplicates.",
          kp: ["matching rows on a condition", "inner drops unmatched rows", "left keeps all left rows null-padded", "row multiplication on one-to-many"] },
        { q: "What is an API and how do you design a simple one?",
          a: "An API exposes a system's capabilities over HTTP in a consistent, documented way. Good API design: clear resource naming (plural nouns, predictable URLs), consistent status codes and error shapes, input validation on every endpoint, versioning from day one, and documentation consumers can rely on. A minimal API is a route, a handler that validates and processes, and a serialized response.",
          kp: ["clear resource naming", "consistent error and status shapes", "input validation", "versioning", "documentation", "serialized responses"] },
        { q: "What's the difference between a process and a thread?",
          a: "A process is an isolated program instance with its own memory space; a thread is a unit of execution within a process that shares the process's memory. Threads are cheaper to create and communicate (shared memory), but shared state brings race conditions. Processes are safer (crash isolation, no shared state) at the cost of heavier communication. In server code this maps to concurrency models: threads, event loops, or processes per request.",
          kp: ["process has own memory space", "threads share process memory", "cheaper context switching for threads", "race conditions from shared state", "crash isolation of processes"] }
      ],
      mid: [
        { q: "Explain database indexing. When does it help and when does it hurt?",
          a: "An index is a structure (usually a B-tree) that lets the database find rows without scanning the whole table — it speeds up lookups, range queries, and joins dramatically. It hurts writes (each insert/update must maintain the index), and it only helps when the query can use it, so you verify with `EXPLAIN`. Composite indexes depend on column order; the most selective column goes first. Covering indexes can serve queries entirely from the index.",
          kp: ["B-tree speeds lookups", "write amplification on inserts", "explain plans to verify", "composite index column order", "covering indexes", "index selectivity"] },
        { q: "What is idempotency and how do you implement it in an API?",
          a: "An idempotent operation can be retried and produce the same result. In practice: clients send an idempotency key, the server deduplicates by that key (unique constraint in the DB), and retries with the same key return the original result instead of double-processing. This matters for payments, order creation, and any operation with side effects. Combined with retry-with-backoff, it gives at-least-once delivery without duplicate effects.",
          kp: ["retries produce same result", "idempotency keys from client", "unique constraint for dedupe", "safe retries", "at-least-once semantics"] },
        { q: "Explain authentication vs authorization. Walk through a JWT flow.",
          a: "Authentication answers 'who are you?' — the login step. Authorization answers 'what can you do?' — permission checks after identity is known. A JWT is a signed token with header, payload, and signature: the server signs it with a secret, the client stores it, and subsequent requests include it so the server can verify without a session lookup. JWTs are stateless but hard to revoke, so pair a short-lived access token with a refresh token, and keep the signing key secure.",
          kp: ["authn is identity authz is permissions", "JWT header payload signature", "stateless verification", "short-lived access plus refresh token", "signing key security", "revocation challenges"] },
        { q: "Explain the CAP theorem with a real example.",
          a: "CAP says a distributed system can guarantee only two of Consistency, Availability, and Partition tolerance under a network partition — and partitions are inevitable, so you choose between consistency and availability when they happen. DynamoDB-style systems favor availability and eventual consistency; HBase/ZooKeeper-style systems favor consistency and may refuse requests during partitions. Real systems blend: 'consistent reads at the cost of availability during an outage' vs 'serve stale data rather than fail'.",
          kp: ["consistency availability partition tolerance", "partitions are inevitable", "AP systems eventual consistency", "CP systems refuse during partition", "tradeoffs are real decisions"] },
        { q: "What is a message queue and when would you use one?",
          a: "A message queue decouples producers from consumers: a producer publishes, a broker stores, and consumers process asynchronously. Use it to smooth load spikes, decouple services (order service publishes 'order created', email service consumes it), and add retry/backpressure. Tradeoffs: at-least-once delivery means consumers must be idempotent, and you add operational complexity and latency. Don't reach for a queue when a simple synchronous call or a database table works.",
          kp: ["decouples producer and consumer", "smooths load spikes", "asynchronous processing", "at-least-once delivery", "idempotent consumers", "don't over-engineer"] }
      ],
      senior: [
        { q: "Design a rate limiter for a public API.",
          a: "Choose an algorithm: token bucket is simple and allows bursts; sliding window is smoother. Track counters per key (user or IP) in a fast store like Redis with TTLs, and enforce at the gateway or middleware. On violation return 429 with a Retry-After header and meaningful error messages. For distributed enforcement, use atomic Redis ops (INCR + EXPIRE) or a Lua script. Monitor quota usage so you can detect abuse and tune limits, and make sure the limiter itself degrades gracefully — never the single point of failure.",
          kp: ["token bucket or sliding window", "per-user or per-IP keys", "Redis counters with TTL", "429 with retry-after", "distributed atomic operations", "monitoring and graceful degradation"] },
        { q: "Your production database is getting slow. How do you approach it?",
          a: "Find the slow queries first — don't guess. Enable slow query logs, run EXPLAIN ANALYZE on the offenders, and check for missing or misused indexes, lock contention, and full scans. Then the usual ladder: add targeted indexes, cache hot reads, offload analytics to a replica, and only then consider partitioning or sharding, which add real complexity. Watch connection pool sizing too — a common cause of 'the database is slow' is app-side exhaustion.",
          kp: ["find slow queries first", "explain analyze", "targeted indexes", "cache hot reads", "replicas for analytics", "connection pool sizing", "sharding only as last resort"] },
        { q: "Explain eventual consistency and how you handle stale reads.",
          a: "Eventual consistency means replicas converge over time rather than immediately — reads may briefly see old data. You handle it by matching consistency to the operation: read-your-writes for the current user (route reads to the replica that saw the write, or wait for acknowledgment), versioning to detect conflicts, and explicit staleness windows in the UI ('updating…'). Strong consistency stays for money and identity; everything else gets a freshness budget.",
          kp: ["replicas converge asynchronously", "read-your-writes consistency", "versioning and conflict detection", "staleness windows in UX", "strong consistency for critical data"] },
        { q: "Design a background job system.",
          a: "Core pieces: a queue (Redis or a broker), workers that consume jobs, and a retry policy with exponential backoff and a dead-letter queue for poison messages. Jobs should be idempotent so retries are safe, and workers need concurrency limits and backpressure so a flood of jobs doesn't collapse the database. Add scheduling for cron-like jobs, observability on queue depth, age, and failure rates, and a replay mechanism. Design the job payload as a durable command, not a pointer to mutable state.",
          kp: ["queue with retries and backoff", "dead-letter queue", "idempotent job handlers", "concurrency and backpressure", "scheduling", "observability of queue depth", "replay and re-drive"] },
        { q: "How do you ensure your API is secure and performant under load?",
          a: "Security: authenticate and authorize every request, validate and limit input, rate limit, and follow the OWASP basics (no injection, safe error messages, HTTPS everywhere). Performance: profile with load tests to find the bottleneck, cache aggressively at the right layer, add pagination and field selection to big endpoints, and use connection pooling and async I/O. Then verify: load test with realistic traffic, set latency SLOs, and monitor in production with traces.",
          kp: ["authn and authz on every route", "input validation and OWASP basics", "rate limiting", "load test to find bottlenecks", "caching at the right layer", "pagination and field selection", "latency SLOs with monitoring"] }
      ],
      staff: [
        { q: "Design a payments system that must never lose money.",
          a: "The invariants: every operation is idempotent (unique constraint on idempotency keys), and money movement lives in an append-only ledger with double-entry accounting — every debit has a credit, so the books always balance. External providers are reconciled against our ledger asynchronously, with mismatch alerts. The system is designed for compensating transactions, not mutation: if a charge fails after a hold, you release, never delete. Add an immutable audit trail, separate money paths from the main CRUD app, and make the ledger the source of truth that everything else derives from.",
          kp: ["idempotency with unique constraints", "append-only double-entry ledger", "reconciliation with external systems", "compensating transactions", "immutable audit trail", "ledger as source of truth"] },
        { q: "How do you migrate a monolithic database to a distributed or sharded architecture with zero downtime?",
          a: "The pattern is dual-write plus backfill: write new records to both old and new stores, backfill historical data with a verification pass that compares counts and checksums, then flip reads behind a feature flag once the new store is consistent. Cut over gradually — move a percentage of traffic — and keep a tested rollback that stops writes to the new store and resumes the old. Run reconciliation jobs for a period after cutover, and have a clear owner and runbook for every step.",
          kp: ["dual-write pattern", "backfill with verification", "feature-flag read cutover", "gradual traffic ramp", "tested rollback plan", "post-cutover reconciliation"] },
        { q: "Design an event-driven architecture. How do you guarantee exactly-once processing?",
          a: "Events become the source of truth; services communicate by publishing and subscribing. Exactly-once is impossible in distributed systems, so you implement effectively-once: at-least-once delivery plus idempotent consumers that deduplicate by event ID (unique constraint or a processed-events store). The outbox pattern guarantees reliable publishing — write the event in the same transaction as the business change, then a relay publishes it. Add a schema registry for evolution, dead-letter queues for poison events, and monitor lag so replay is easy.",
          kp: ["effectively-once via idempotency", "dedupe by event ID", "outbox pattern for reliable publish", "schema registry and versioning", "dead-letter queues", "lag monitoring and replay"] },
        { q: "Design a globally distributed system serving millions of users.",
          a: "Start from latency and residency: place data and compute near users, with regions chosen for regulation. Choose a replication model — active-active for reads and local writes, active-passive for simplicity — and define conflict resolution (last-write-wins for most, CRDTs or business rules where it matters). Global load balancing via DNS/anycast routes users to the nearest region. Add automated failover with periodic DR drills, and understand the real cost: cross-region replication is expensive, so replicate deliberately, not everything-everywhere.",
          kp: ["data residency and latency", "active-active vs active-passive", "conflict resolution strategy", "global DNS and anycast", "automated failover and DR drills", "replication cost management"] }
      ],
      principal: [
        { q: "Design the backend platform strategy for a company scaling 10x.",
          a: "The platform exists to make delivery safe and fast for product teams: a standardized core stack, self-service infrastructure (deploy, databases, queues through an internal platform), and enforced contracts and API governance. Define SLOs and error budgets so reliability is a decision input, not a surprise. Scale the org with the system: a platform team that treats product teams as customers, with a small number of golden paths instead of infinite flexibility. Sequence it as: standardize, then automate, then measure — never all at once.",
          kp: ["standardized core stack", "self-service internal platform", "API governance and contracts", "SLOs and error budgets", "platform team as internal product", "golden paths over flexibility"] },
        { q: "How do you decide when to build vs buy (databases, queues, ML infra)?",
          a: "Frame it as core vs context: if the capability is a competitive advantage, build; otherwise buy or use managed services. Then look at total cost of ownership — purchase cost plus operations, on-call, and expertise — not just license fees. Evaluate exit costs and lock-in: can we leave if it fails? Match maturity: a young team shouldn't run its own database. And always keep the decision reversible where possible.",
          kp: ["core vs context competency", "total cost of ownership", "managed services for maturity", "exit costs and lock-in", "time to market", "operational capacity to run it"] },
        { q: "A critical service has a 99.99% SLO but your team keeps getting paged. How do you fix reliability culture?",
          a: "Start with the evidence: what's actually consuming on-call time? Reduce toil with automation and fix the alerting — alerts should be actionable and meaningful, or they train people to ignore them. Then make error budgets real: if the budget is spent, releases slow down by policy, which forces product tradeoffs into the open. Blameless postmortems with action items and owners convert incidents into improvement. Finally, rotate on-call fairly with training and shadowing so reliability is a shared skill, not a punishment.",
          kp: ["measure on-call toil", "meaningful actionable alerting", "error budgets gate releases", "blameless postmortems with owners", "fair on-call rotation with training", "reduce toil with automation"] },
        { q: "How do you set up architecture governance that engineers actually follow?",
          a: "Governance works when it's lightweight and embedded: ADRs (architecture decision records) capture decisions with context, a small review board handles only the decisions that matter (skip bikeshedding), and guardrails are automated in CI — lint rules, dependency policies, cost budgets — so compliance is a side effect of normal work. Documentation-as-code keeps the system current, and a clear exception process means nobody has to subvert the rules. Above all: evangelize through working examples, because people follow what they see working.",
          kp: ["ADR decision records", "lightweight review boards", "automated guardrails in CI", "documentation as code", "explicit exception process", "evangelize through examples"] }
      ]
    }
  },
  {
    id: "fullstack", name: "Full-Stack Engineer", icon: "🧩",
    blurb: "End-to-end product delivery across client and server.",
    skills: ["Frontend + backend", "APIs & data", "Auth & real-time", "Product thinking"],
    questions: {
      junior: [
        { q: "Walk me through a typical request from frontend to database and back.",
          a: "A user action in the UI triggers a fetch to an API endpoint. The server's router matches the path to a handler, which validates input, runs any business logic, queries the database, and serializes the result as JSON. The response flows back over HTTP, and the frontend updates its state and re-renders. Every layer is a place to add caching, validation, or error handling.",
          kp: ["UI event triggers fetch", "router matches handler", "validation and business logic", "database query", "JSON response", "state update and re-render"] },
        { q: "What is CORS and why do browsers enforce it?",
          a: "CORS (Cross-Origin Resource Sharing) is a browser security mechanism: a page at origin A can't read responses from origin B unless B explicitly allows it via the Access-Control-Allow-Origin header. Browsers enforce it to prevent malicious sites from making credentialed requests to sites you're logged into. For complex requests the browser sends a preflight OPTIONS request. The fix belongs on the server: configure allowed origins explicitly, never a wildcard with credentials.",
          kp: ["same-origin policy", "browser-enforced security", "access-control-allow-origin header", "preflight OPTIONS requests", "credentials handling", "server-side configuration"] },
        { q: "What is JSON and how is it used in web APIs?",
          a: "JSON is a lightweight text format for structured data: objects of key-value pairs, arrays, and primitive values. It's the de facto API format because it's human-readable and natively parseable by JavaScript. APIs serialize server data to JSON, clients parse it, and tools like JSON Schema validate its shape. Design matters: consistent field naming, explicit null vs absent, and never returning sensitive fields by default.",
          kp: ["key-value text format", "serialization and parsing", "application/json content type", "nested structures and arrays", "schema validation", "consistent field naming"] },
        { q: "Explain environment variables and why secrets should never be in code.",
          a: "Environment variables keep configuration outside the codebase, so the same code runs in dev, staging, and prod with different values. Secrets like API keys and DB passwords must never be committed: anything in git is effectively public forever, and leaked credentials get scraped by bots within minutes. Use .env files locally, a secret manager in production, inject at deploy time, rotate regularly, and give each service only the credentials it needs.",
          kp: ["config outside code", "dev staging prod parity", "secrets never in git", "secret managers", "rotation and least privilege", "injected at deploy time"] }
      ],
      mid: [
        { q: "Explain sessions vs tokens for authentication in a full-stack app.",
          a: "Sessions store state server-side, hand the client a cookie, and are easy to revoke — but they need sticky sessions or a shared store at scale. Tokens (JWTs) are stateless and scale horizontally trivially, but revocation is hard and size grows. Common approach: short-lived access token for API calls plus a refresh token, or session-backed auth for classic web apps. The right answer depends on your clients: cookie sessions suit server-rendered web apps; tokens suit SPAs, mobile, and third-party integrations.",
          kp: ["server-side sessions via cookie", "stateless JWT tokens", "revocation tradeoffs", "CSRF vs XSS concerns", "mobile and third-party clients", "hybrid short-lived token design"] },
        { q: "How do you keep client and server data in sync?",
          a: "Treat server data as a cache the client owns: a data-fetching library (React Query, SWR) manages keys, caching, refetching, and invalidation instead of hand-rolled fetch logic. Optimistic updates apply the change immediately and roll back on failure. For real-time needs, add WebSockets or SSE for targeted updates. Offline-first apps keep a local store as source of truth and sync mutations through a queue with conflict resolution.",
          kp: ["server state as cache", "fetching libraries manage invalidation", "optimistic updates with rollback", "websockets or SSE for real-time", "offline queue and sync", "stale-while-revalidate"] },
        { q: "What is a webhook? How do you secure and debug them?",
          a: "A webhook is an HTTP callback: one service POSTs an event to a URL another service owns. Because the receiving URL is public, you must verify authenticity — sign the payload with HMAC using a shared secret and check it on receipt, plus require HTTPS. Handlers must be idempotent because providers retry with backoff. Debugging: log raw payloads, use a webhook testing tool locally, and keep replay endpoints for reproducing events.",
          kp: ["HTTP callback for events", "HMAC signed payloads", "verify signature on receipt", "idempotent handlers with retries", "HTTPS required", "replay and debugging tools"] },
        { q: "Design a full-stack feature: profile editing with image upload.",
          a: "Frontend: a form with client-side validation and optimistic save state. Upload: send the image via multipart to a signed endpoint or presigned URL (direct to object storage), validating type, size, and dimensions server-side — never trust client checks. Store the metadata (URL, size, user id) in the database, serve through a CDN with cache-busting on the URL. Security: authenticate every mutation, guard against path traversal by using storage keys you generate, and handle upload failures with clear UX.",
          kp: ["client and server validation", "presigned URL or multipart upload", "object storage and CDN", "authenticate every mutation", "generate storage keys server-side", "progress and error UX"] }
      ],
      senior: [
        { q: "Design a real-time collaborative editor like Google Docs (conceptual).",
          a: "The core problem is concurrent edits converging to one document. Two families of solutions: Operational Transformation (used by Google Docs) or CRDTs, both giving eventual consistency without data loss. WebSockets carry updates; the server acts as the coordinator and source of truth; clients apply transformations and resolve conflicts locally. Beyond the core: presence and cursors, permission checking per edit, offline support with a local change log, and storage snapshots with an undo/version history.",
          kp: ["operational transformation or CRDTs", "websocket transport", "server as source of truth", "conflict resolution", "presence and cursors", "offline change log and sync"] },
        { q: "How do you handle authentication across web, mobile, and third-party apps?",
          a: "Use OAuth2 with the right flow per client: authorization code with PKCE for mobile and SPAs, authorization code for server-side web apps, client credentials for machine-to-machine. SSO via an identity provider (Okta, Auth0, Google) centralizes identity. Issue short-lived access tokens with rotating refresh tokens, and keep revocation centralized in the auth service. The auth service itself must be boring and well-audited — it's the crown jewels.",
          kp: ["OAuth2 flows per client", "PKCE for mobile and SPAs", "SSO with identity providers", "refresh token rotation", "centralized auth service", "central revocation"] },
        { q: "Design a feature that must work offline-first.",
          a: "The local store (IndexedDB/SQLite) is the source of truth; the server is a synchronization target. Reads come from the local store, writes go into an outbox queue and sync in the background with retry. Conflicts need per-data-type strategies: last-write-wins for simple fields, merge for lists, and versioned objects with user resolution for important documents. Schema versioning and migrations are mandatory since devices update at their own pace. The UI must communicate sync state honestly — 'saved locally, syncing'.",
          kp: ["local store as source of truth", "outbox queue with retry", "background sync", "per-type conflict resolution", "schema versioning and migrations", "honest sync state UX"] },
        { q: "How do you reduce end-to-end latency for users on slow networks?",
          a: "Shrink what travels and move it closer: CDN and edge caching for static and API responses, HTTP/2 or HTTP/3 with compression, and smaller payloads (pagination, field selection, or batching). Render fast first — server-side rendering or streaming for first paint, service worker precaching for repeat visits. Give the UI graceful fallbacks: skeletons, cached data shown immediately, and offline-tolerant retries instead of blank errors.",
          kp: ["edge caching and CDN", "HTTP/2 and compression", "smaller payloads and batching", "SSR for first paint", "service worker precaching", "graceful fallbacks and skeletons"] }
      ],
      staff: [
        { q: "Design a multi-tenant SaaS platform where each customer needs isolation.",
          a: "Choose an isolation model per data sensitivity: shared schema with tenant_id + row-level security is cheapest; schema-per-tenant or DB-per-tenant buys stronger isolation for enterprises. Every query, cache key, and background job must be tenant-scoped — this is where leaks happen. Add tenant-scoped auth and rate limits so one noisy neighbor can't take you down, and handle compliance and data residency per tenant (a tenant's data stays in its region). Expose an admin console with per-tenant health, usage, and kill switch.",
          kp: ["isolation models shared schema to db per tenant", "tenant-scoped queries and cache keys", "row-level security", "noisy neighbor mitigation", "per-tenant data residency", "tenant kill switches"] },
        { q: "Design a feature-flag and experimentation platform.",
          a: "Feature flags need ultra-low-latency evaluation — cache the flag rules at the edge and in the SDK so the flag service is never on the request path. Target by user, cohort, or percentage for progressive rollouts and instant kill switches. Experimentation layers on top: deterministic assignment (hash of user id), event collection, and statistically sound analysis with guardrail metrics. The unsung work is hygiene: flag ownership, expiry, and cleanup so flags don't become permanent dead code.",
          kp: ["edge evaluation and SDK caching", "targeting rules and cohorts", "deterministic A/B assignment", "guardrail metrics", "kill switches and progressive rollout", "flag hygiene and expiry"] },
        { q: "Design a search feature across a large product catalog.",
          a: "Index documents in a search engine (Elasticsearch/OpenSearch or a vector DB for semantic search) using an inverted index. The ingestion pipeline tokenizes, normalizes, and enriches documents, then reindexes on changes with alias swaps for zero-downtime updates. Ranking combines relevance (BM25, embeddings) with business rules (popularity, freshness). Add faceted filters, typo tolerance, and pagination/search-as-you-type, and measure quality with click-through on results — search quality is a feedback loop, not a one-time build.",
          kp: ["inverted index", "ingestion and tokenization pipeline", "relevance ranking BM25 or embeddings", "facets and filters", "typo tolerance", "reindexing with alias swaps", "measure via click-through"] },
        { q: "How do you evolve an API from v1 to v2 without breaking clients?",
          a: "Prefer additive changes forever: new fields, new endpoints, never removing or changing meaning. When breaking changes are unavoidable, run both versions with a clear deprecation policy — announce timeline, keep v1 alive long enough, and give migration guides and tooling. Version via URL or header consistently, and use compatibility tests that lock the old behavior. Track v1 usage metrics so you know when it's truly dead, then sunset it.",
          kp: ["additive changes first", "deprecation policy with timeline", "consistent versioning strategy", "compatibility tests", "migration guides and tooling", "usage metrics before sunset"] }
      ],
      principal: [
        { q: "Design the developer experience platform for 100+ engineers shipping full-stack.",
          a: "The platform's job is to make the golden path the easy path: project scaffolding with best practices baked in, CI/CD templates that work on day one, ephemeral preview environments, and built-in observability (logs, traces, dashboards) in every service. Local development parity with production is a force multiplier. Everything is measured: time-to-first-deploy for new hires, build times, flake rates, developer satisfaction surveys — the platform competes on developer productivity like a product competes on revenue.",
          kp: ["scaffolding and templates", "CI/CD golden paths", "preview environments", "observability built in", "local dev parity", "measure developer productivity"] },
        { q: "One team ships 3x faster with terrible quality; another is slow but excellent. What's your strategy?",
          a: "Both are symptoms, so measure both axes before prescribing. The fast team probably has no safety net — invest in automated tests, deploy safety (canaries, rollbacks), and code review. The slow team is probably bottlenecked on process or fear — attack review latency, break large changes into smaller deploys, and add test coverage so confidence comes from automation instead of caution. Align incentives: reward shipping with quality, not either alone, and make the deploy pipeline the safety net both teams trust.",
          kp: ["measure speed and quality separately", "shift quality left with automation", "deploy safety canary rollback", "reduce review latency", "small frequent deploys", "align incentives on both axes"] },
        { q: "How do you design for scale: from 1k to 10M users?",
          a: "Design for the 10M shape but don't build it on day one: stateless services that scale horizontally, a cache layer that absorbs read spikes, async processing for anything non-critical, and data modeled so it can shard later. The discipline is avoiding premature optimization — you optimize the architecture that survives, not the one that's hypothetical. Load test early to find the real bottlenecks, and let the database be the last thing you shard, not the first.",
          kp: ["stateless horizontally scalable services", "cache layers", "async where possible", "shardable data model", "avoid premature optimization", "load test to find real bottlenecks"] },
        { q: "Define API governance and standards for the whole company.",
          a: "Standards should make the right thing automatic: naming conventions, pagination, error shapes, and idempotency documented once and enforced by linters and CI checks rather than review nagging. A schema registry governs evolution, and every API has an owner with an SLO. Security review is automated and gated. The governance body stays small and pragmatic — it approves exceptions and evolves the standards — because the goal is consistency that lets engineers move between teams without relearning everything.",
          kp: ["naming and error conventions", "automated linting in CI", "schema registry", "owner and SLO per API", "automated security review", "pragmatic exception process"] }
      ]
    }
  },
  {
    id: "devops", name: "DevOps / Cloud", icon: "☁️",
    blurb: "CI/CD, containers, cloud infrastructure and reliability.",
    skills: ["Kubernetes & Docker", "AWS · GCP · Azure", "CI/CD & IaC", "SRE & observability"],
    questions: {
      junior: [
        { q: "What is a container and how is it different from a VM?",
          a: "A container packages an application with its dependencies and runs isolated processes sharing the host OS kernel — fast startup, small footprint, portable. A VM virtualizes the hardware: each VM runs a full guest OS on a hypervisor, so it's heavier but has stronger isolation. In practice: containers for application workloads, VMs when you need a full OS or kernel-level isolation. Containers made 'works on my machine' obsolete because the image is the environment.",
          kp: ["packages app with dependencies", "shares host kernel", "isolated processes", "fast startup vs VMs", "image is the environment"] },
        { q: "Explain CI and CD. What's the difference?",
          a: "Continuous Integration means merging code frequently and automatically building and testing on every change, so integration problems surface early. Continuous Delivery means every change is automatically deployed to staging and ready to go to production, with the production deploy automated as well. The payoff: small, reversible changes instead of big-bang releases, and fast feedback to developers.",
          kp: ["merge frequently", "build and test on every push", "catch integration problems early", "automated deploy to staging", "production deploys automated", "small reversible changes"] },
        { q: "What is Infrastructure as Code and why does it matter?",
          a: "IaC means defining infrastructure — networks, servers, databases — in configuration files (Terraform, CloudFormation, Pulumi) instead of clicking in a console. Infrastructure becomes version-controlled, reviewable, and reproducible: you can rebuild an environment from scratch, review changes in pull requests, and detect drift. It turns 'what's running where?' from tribal knowledge into a codebase, and it's the prerequisite for treating infrastructure like software.",
          kp: ["define infrastructure in config", "version-controlled", "reviewable in pull requests", "reproducible environments", "drift detection", "terraform and cloudformation"] },
        { q: "Walk me through debugging a container that won't start.",
          a: "Start with the logs — `docker logs` or the orchestrator's logs — they usually tell you what's wrong. Verify the image and tag exist and match, check environment variables and secrets are present, and confirm ports and health checks line up. Look at resource limits (OOM kills) and whether dependencies like the database are reachable. Compare against the last known good version; if nothing else, `docker run` it manually with the same config to reproduce locally.",
          kp: ["check logs first", "verify image and tag", "environment and secrets present", "ports and health checks", "resource limits and OOM", "compare to last known good"] }
      ],
      mid: [
        { q: "Design a CI/CD pipeline for a web application.",
          a: "Stages: lint and format, unit tests, build, and image creation, with dependency caching so runs stay fast. Deploy to a staging environment automatically, run integration and e2e tests there, then promote to production — either on merge with a canary, or behind an approval gate for larger teams. Every stage needs a rollback story, and the pipeline itself is code, reviewed like any other change. Secrets are injected from a secret manager, never stored in the repo.",
          kp: ["lint test build stages", "dependency caching", "artifact and image build", "staging before production", "approval gates or canary", "rollback strategy", "secrets from secret manager"] },
        { q: "How do you deploy a service with zero downtime?",
          a: "The standard options: rolling deployment (new instances come up, health-checked, then old ones drain and shut down), blue-green (a whole new environment is validated then traffic flips), and canary (a small percentage of traffic hits the new version, watched on metrics, then ramped). All of them need health checks, connection draining, and a fast rollback. The database is the hard part — schema migrations must be backward-compatible so old and new code run against the same schema.",
          kp: ["rolling deployment", "blue-green environment swap", "canary with metrics and ramp", "health checks and readiness", "connection draining", "backward-compatible migrations"] },
        { q: "Explain requests vs limits for CPU and memory in Kubernetes.",
          a: "Requests are what the scheduler guarantees and uses for placement; limits are hard caps. CPU is compressible — a pod over its CPU limit gets throttled — while memory is not: exceeding the memory limit gets the pod OOM-killed. Requests and limits together define Kubernetes Quality of Service classes (Guaranteed, Burstable, BestEffort). Set requests from real usage (with headroom), and think about oversubscription: limits above requests let you pack more pods but risk noisy neighbors.",
          kp: ["requests guarantee scheduling", "limits are hard caps", "cpu compressible memory is not", "OOM kills", "quality of service classes", "oversubscription tradeoffs"] },
        { q: "How do you monitor a system? What would you alert on?",
          a: "Cover the three pillars: metrics, logs, and traces. For services, the RED method (Rate, Errors, Duration); for infrastructure, USE (Utilization, Saturation, Errors). Define SLOs from user experience and alert on error budgets — alert on symptoms (users affected) with runbooks, not on causes (you'll get there during investigation). Every alert must be actionable and every page must have a runbook; if an alert doesn't lead to action, delete it.",
          kp: ["metrics logs traces", "RED method for services", "USE method for infrastructure", "SLOs and error budgets", "alert on symptoms with runbooks", "avoid alert fatigue"] }
      ],
      senior: [
        { q: "Design a Kubernetes deployment for a high-availability web service.",
          a: "A Deployment with multiple replicas spread across nodes and availability zones (pod anti-affinity, topology spread), managed by an HPA scaling on real metrics like request rate or CPU. Readiness probes gate traffic, liveness probes restart dead pods, and a PodDisruptionBudget ensures voluntary node drains don't drop availability. Stateless app in front of a managed database (state doesn't live in pods), with network policies restricting east-west traffic and a Service/Ingress exposing it. Everything is GitOps-managed so the cluster state is declarative.",
          kp: ["replicas with anti-affinity and zone spread", "HPA on real metrics", "readiness and liveness probes", "pod disruption budgets", "stateless apps with managed state", "network policies and ingress", "gitops declarative state"] },
        { q: "How do you set up observability across microservices?",
          a: "Correlation is the hard part: propagate a trace/request ID through every service boundary (headers), then logs, metrics, and traces can be joined. Centralize logs with structured JSON and searchable fields; export metrics with consistent labels to a time-series store; and add distributed tracing to see the full request path with per-span latency. Every service publishes golden signals, and dashboards are organized per service with an error budget view on top. Add alerting with runbooks that reference the dashboards.",
          kp: ["correlation and trace IDs", "propagate context via headers", "structured centralized logs", "metrics with consistent labels", "distributed tracing spans", "golden signals per service", "runbooks tied to alerts"] },
        { q: "Design a disaster recovery plan.",
          a: "Define the targets first: RPO (how much data can we lose — drives backup frequency) and RTO (how fast must we recover — drives failover design). Then the mechanics: automated backups with verified restores (test them regularly, untested backups are rumors), cross-region replication for critical data, and a documented failover runbook that's actually rehearsed in game days. Decide failover mode: active-passive (cheaper, slower RTO) vs active-active (expensive, near-zero RTO). Include a communication plan: who declares the incident, who tells customers, and when you fail back.",
          kp: ["RPO and RTO targets", "verified backup restores", "cross-region replication", "rehearsed failover runbooks", "active-active vs active-passive", "communication and failback plan"] },
        { q: "How do you keep secrets secure in a cloud environment?",
          a: "Centralize secrets in a secret manager (AWS Secrets Manager, Vault) with rotation automation — never in code, env files, or image layers (those get baked into artifacts forever). Enforce least privilege with IAM: services get scoped roles, short-lived credentials, and no standing keys where possible. Encrypt at rest and in transit everywhere, enable audit logging on secret access, and run periodic reviews of who can access what. Treat a leaked secret as an incident: rotate immediately and find how it leaked.",
          kp: ["centralized secret manager", "automated rotation", "least privilege IAM roles", "short-lived credentials", "audit logging", "encryption at rest and transit"] }
      ],
      staff: [
        { q: "Design the platform engineering team to serve 50 squads.",
          a: "The team succeeds by making infrastructure a product, not a ticket queue: golden paths and paved roads — a handful of blessed, fully supported ways to build, deploy, and run services — with self-service access so squads never wait on a human. An internal developer portal (backstage-style) exposes templates, docs, and environment management. Guardrails are policy-as-code, enforced automatically, not by review. The platform team measures itself on developer throughput, cost, and reliability, and it runs its own services with the same standards it preaches.",
          kp: ["golden paths and paved roads", "self-service infrastructure", "internal developer portal", "policy as code guardrails", "product mindset with metrics", "dogfooding the platform"] },
        { q: "How do you scale Kubernetes to thousands of nodes and tens of thousands of pods?",
          a: "At that scale you run many clusters (regional or per-team) rather than one giant one, managed through a federation or fleet layer. Inside clusters: namespace quotas and resource governance so one team can't starve others, node autoscaling plus pod autoscaling tuned to real demand, bin-packing to keep utilization high, and cluster autoscaler limits. Watch the control plane — API server load, etcd limits — and use vertical scaling of control-plane components. Network and DNS become bottlenecks: efficient service discovery and CNI choices matter. And cost management is a feature, not an afterthought.",
          kp: ["many clusters via federation", "namespace quotas and governance", "node and pod autoscaling", "control plane scaling limits", "network and DNS bottlenecks", "cost management built in"] },
        { q: "Design a multi-cloud or hybrid strategy. When does it make sense?",
          a: "Multi-cloud is usually a cost and complexity multiplier, so the default is one cloud done well. It makes sense when: regulatory requirements demand specific regions/vendors, you need resilience against a cloud-wide outage (rare, and active-active multi-cloud is brutally hard), or you're acquiring companies with different footprints. Hybrid (on-prem + cloud) is common for data gravity or compliance. If you go multi-cloud, abstract only the stable layers (containers, IaC, Kubernetes) and accept that you'll run everything twice. Have an exit strategy either way: portable infrastructure is cheap insurance.",
          kp: ["single cloud is the default", "regulatory and data residency drivers", "active-active multi-cloud complexity", "hybrid for data gravity", "abstraction layers only where stable", "portability as exit insurance"] },
        { q: "You're on call for a major platform outage. Walk me through it.",
          a: "First, stabilize: stop the bleeding with the fastest safe action — rollback, failover, or scaling out — before deep investigation. Communicate immediately and continuously: a status page, a stakeholder thread, and clear ownership of the incident. Then gather evidence with timelines while it's fresh. After recovery: a blameless postmortem that asks 'what in the system allowed this?' not 'who did it?', with action items, owners, and dates. The final step is making sure the fix actually prevents recurrence and that the runbook reflects what we learned.",
          kp: ["stabilize before investigate", "fast rollback or failover", "continuous stakeholder communication", "evidence and timeline", "blameless postmortem", "action items with owners", "prevent recurrence"] }
      ],
      principal: [
        { q: "Define the reliability and SLO strategy for the company.",
          a: "SLOs must be defined from user journeys, not infrastructure internals: 'sign-in works in under 2s 99.9% of the time' beats 'API latency p99'. Tier the targets — core money paths get tight SLOs, experimental features get room to breathe. Error budgets turn reliability into an engineering decision: when the budget is spent, you slow releases and fix, which forces product tradeoffs into the open. Measure burn rate so you act on trends, not only after the quarter is gone. And build the culture: blameless reviews, runbooks, and on-call that's respected, not resented.",
          kp: ["SLOs from user journeys", "tiered targets by criticality", "error budgets gate change", "burn rate monitoring", "blameless culture", "reliability as shared ownership"] },
        { q: "Design cloud cost optimization as a program, not a one-off.",
          a: "Make cost visible first: mandatory tagging with showback or chargeback so every team sees its bill and owns it. Then attack the structural wins: rightsizing (match instance sizes to actual usage), commitment discounts for stable workloads, idle resource cleanup (stale environments, orphaned volumes), and storage lifecycle policies. Build FinOps culture: cost reviews in planning, budgets and anomaly alerts, and celebrate savings like revenue. Automate the boring part — policy-as-code that deletes or downsizes — so savings don't depend on discipline alone.",
          kp: ["tagging and showback chargeback", "rightsizing and commitments", "idle resource cleanup", "budgets and anomaly alerts", "finops culture and incentives", "automated remediation"] },
        { q: "How do you evaluate a new technology for the platform (a new database, a new cloud service)?",
          a: "Start with requirements and constraints, not the shiny thing: what problem are we solving, what are the operational and security requirements? Run a POC against real workloads with realistic data — synthetic benchmarks lie. Assess operational maturity: can we run, monitor, and troubleshoot this at our scale, and who owns it? Model the cost honestly, including migration and exit costs. Check security and compliance fit. And have an exit strategy: a decision without an off-ramp is a bet you can't fold.",
          kp: ["requirements before technology", "POC with real workloads", "operational maturity and ownership", "honest cost model", "security and compliance review", "exit strategy"] },
        { q: "How do you build security into the delivery pipeline?",
          a: "Shift left: static analysis (SAST) and dependency scanning run in CI on every merge, and secrets scanning catches credentials before they land in git. Images are scanned and signed, and only signed images deploy. At runtime, add detection: anomaly monitoring, intrusion detection, and audit logging. Policy-as-code (Open Policy Agent-style) enforces standards automatically — no human gate to be skipped. And the human layer: penetration tests and red-team exercises on a schedule, with findings tracked like bugs, plus a ready incident response plan.",
          kp: ["SAST and dependency scanning in CI", "secret scanning", "image signing and supply chain", "policy as code", "runtime detection and audit", "pen tests and incident response"] }
      ]
    }
  }
];
