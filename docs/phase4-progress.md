# Phase 4 — Progress & Session Memory

> Running record for the Phase 4 consolidated plan (`docs/phase4-enhancements-plan.md`).
> Cadence per item: plan → owner-approve → build (5 gates) → PR → merge → owner SQL step.
> Baselines move with each merged item — the table below is authoritative.

## Status snapshot (2026-09-24) — PHASE 4 COMPLETE

| Item | Scope | PR | State | Owner action after merge |
|---|---|---|---|---|
| A — Q-bank metadata & filters | `skills` column, Added chip, skill/status filters | #66 | **Merged + migrated + deployed** | done (`content-sourcing.sql` applied 2026-09-23) |
| B — Scraper run reports + GH Actions cron log | `scraper_runs`, run-log & cron-log cards | #67 | **Merged + migrated + deployed** | done (`scraper-runs.sql` applied 2026-09-23) |
| C — Playwright job scraping | `jobs` source `playwright`, targets data, daily workflow | #68 | **Merged + verified live** (34 jobs; YC 14, WWR 20) | none |
| D1 — Pure discovery engines | `discover-lib.js`, `crawl-lib.js` (zero I/O) | #69 | **Merged** | none |
| D3 — Takedown engine (standalone) | soft-delete + suppressions + audit + admin UI | #72 | **Merged + migrated + deployed** | done (`discovery.sql` applied 2026-09-23, incl. D2 tables) |
| — hotfix: oversize upsert + ai-clean scope | batch-poisoning fix, cron AI-clean now works | #71 | **Merged** | none |
| — hotfix: missing `yellow()` helper | nightly crashed after extraction (no upsert, no report) | #73 | **Merged + verified live** (330 drafts upserted) | none |
| — hotfix: oversize drops are notices | nightly exited 1 forever on a recurring 120k-char item | #74 | **Merged + verified live** (green run, errors=0) | none |
| D2 — Discovery orchestrator + storage | `crawl-sources.js`, `discovery_seeds`/`discovered_resources` | #75 | **Merged + migrated + dry-run verified** (8 pages, 260 resources, 0 errors) | done (`discovery.sql` re-applied 2026-09-24 — see correction below) |
| D4 — Admin discovery UI + credits | Discover-from-URL card, approval queue, credits pages | #76 | **Merged + migrated + live-verified** (approved-only public view) | done (`discovery-public-read.sql` applied 2026-09-24) |
| D5 — Skill-gap auto-discovery + weekly workflow | `discover-skills.js`, `discover-weekly.yml` | #77 | **Merged + verified live** (fix #78; first run queued 17 pending seeds) | none |
| — fix: D5 insert 5 exprs for 6 columns | `buildSeedValuesSql` + regression test | #78 | **Merged + verified live** (re-dispatch queued 17 seeds) | none |
| — crawl-weekly dispatch workflow | the crawler's CI execution lever (dispatch-only) | #79 | **Merged + verified live** (15 pages, 28 drafts, 114 resources, 0 errors) | none |
| — L5 re-validation cron + admin smoke test | `revalidate-resources.js`, `revalidate-weekly.yml` | #80 | **Merged + verified live** (empty-case exit 0) | none |
| — LIVE end-to-end discovery round | approve seeds → crawl → review queue | — | **Done** (see live-round facts below) | none |
| — Draft-quality filter + skill tags | `draft-quality-lib.js`, noise-drop, derived skills | #81 | **Merged + live data cleaned** (20 noise drafts deleted, 8 tagged keepers) | none |
| — Editorial push: draft pile → live bank | 233 answered drafts published with derived skill tags, 6 dupes deleted | — | **Done** (2026-09-25; bank 36 → **269**, tagged 24/36 → **112/269**) | none |
| — Admin skill filters (Question Bank + Review Inbox) | `adminSkillChips` + `publishedMatchesSkill`, skill select on both admin list filters | — | **Built, 5 gates green** (vitest 1449, +2 tests) | PR/merge/deploy |
| — Admin multi-select skill chips + tag backfill | FilterChip extraction, multi-select AND chips on both admin lists, 4 new deriveSkills shelves, `less` fix, 91-row live backfill | #84 | **Merged + deployed** (tagged 112/269 → **203/269, 75%**) | none |
| — Skill chips AND→OR | `matchesAnySkill` replaces `matchesAllSkills` (empty-selection guard); Bank + both admin lists | #85 | **Merged + deployed** (byte-verified) | none |
| — Review Inbox skill chips for the untagged backlog | triage worker derives skills (explicit tags win), 🛠 chips on draft cards, chips row always live, filter uses same source | #86 | **Merged + deployed** (byte-verified) | none |
| — Keyless GitHub REST search for discovery | classifySeed `github-search` kind, `githubSearchUrl`/`parseRepoSearchHit`, REST hits fan into license-checked repo child crawls | #87 | **Merged + deployed** (live smoke: svelte → 3 repos, MIT/Apache-2.0/none) | none |
| — Selector-drift alarm + L5 content markers | zero-yield Playwright runs exit 1; `markerFromText` → `meta.contentMarker` at approval; weekly probe quarantines on a real mismatch only | #88 | **Merged + deployed** (WWR selectors re-verified live) | none |
| — Opt-in Playwright rendering for the crawl | `discovery-render-fetcher.mjs` behind `DISCOVERY_RENDER=1`; plain fetch stays default + fallback | #89 | **Merged + deployed** (crawl-weekly installs Playwright only when the var is set) | none |
| — pdfjs-dist bump | 6.2.108 → 6.3.289 | #90 | **Merged + deployed** (pdf/resume suites unchanged) | none |
| — AI-clean key fix + second editorial push | getunikey key (deepseek-v4-flash after quota dead-ends), 286 drafts cleaned, 281 published with derived tags | — | **Done** (2026-09-26; bank 269 → **550**, pile 293 → 12) | none |
| — Make ATS-ready resume rewrite | 🎯 one-click on ResumeCard: strict-JSON rewrite via `chat(module:"ats")`, contact repair, skill-matched job picker, use-as-my-resume | #91 | **Merged + deployed** (Pages 36220910668 success) | none |
| — Working-model discovery + 2-min auto-apply | `probe-models`/`set-provider-model` actions on the `ai-chat` edge fn; ModelScanCard probes, ranks with task descriptions, auto-applies after 120 s; ai-chat registered in deploy.yml | #92 | **Merged + deployed** (Pages 36222391094 success, ai-chat bundled 776 kB) | none |
| — Model-list CORS fix | ai-chat list/probe/set handlers threaded the prepared CORS headers (bare Responses were browser-blocked 200s); `fetchAvailableModels` throws with the real reason, never caches [] | #93 | **Merged + deployed** (live: GET /models 200 with ACAO, 44 models) | none |
| — Scan stale-report + no-listing fixes | ModelScanCard re-scans when the saved provider changes (+ ↻ Rescan); edge probes fallback candidates when /models unavailable; HTML-200 probes/test-key rejected as non-chat | #94 + 92891c08 | **Merged + deployed** (owner-reported: agentrouter key switch) | none |

Test baselines: Item A 1298 → B 1311 → C 1327 → D1 1345 → #71 1349 → D3 1361 → D2 1385 → **D4 1403 (+18)** → **D5 1416 (+13)** → **#78/#80 1429 (+13)** → **#81 1443 (+14, `draftQuality.test.ts`)** → **#82 1445 (+2, multi-skill filter)** → **#83 1447 (+2, hn-story + topic shelves)** → **#84 1449 (+2, admin skill filters)** → **#85 1451 (+2, OR semantics)** → **#86 1452 (+1, derived inbox chips)** → **#87 1458 (+6, github-search)** → **#88/#89/#90 1459 (+1, marker-verdict tests)** → **#91 1467 (+8, `atsReady.test.ts`)** → **#92 1478 (+11, `aiModelPicker.test.ts`)** → **#93 1486 (+8, `aiModels.test.ts`)** → **#94 1489 (+3, fallback + stale-report tests)** (all with the 5-gate cadence: typecheck → build → vitest → eval:rag 41 → deno 72/0).

## Key decisions & facts (carry into D2/D4/D5)

- **Two report tables, cleanly split.** `scraper_runs` = question scraper + discovery crawler (D2 reports here, trigger `'cron'`). `jobs_fetch_reports` = jobs pipelines (`jobs-fetch` Edge Function + `scrape-jobs-playwright.mjs`). Both write via the management API (service role bypasses RLS); `scraper_runs` also has an admin INSERT policy so browser "Run now" reports survive RLS.
- **First live cron report (2026-09-23) exposed two pre-existing bugs** — both fixed in #71:
  1. One 28KB scraped question poisons the whole multi-row upsert (btree index-entry cap 8191 bytes). `MAX_QUESTION_CHARS=2000` + `partitionOversizeQuestions()` in `scrape-lib.js`; the cron marks the owning source's error in `per_source`.
  2. `ai-clean.js` `cleanOne()` referenced `aiKey/aiBase/aiModel` declared inside `main()` → `ReferenceError` per item; AI cleaning had never run in CI. Config hoisted to module scope.
- **Playwright selectors are verified live (2026-09-23)** but drift fast: YC cards are now `a[href^='/jobs/']` (title `h3 span`, company `p.font-semibold`); WWR is `a.listing-link--unlocked` + `.new-listing__header__title__text` / `.new-listing__company-name`. himalayas + remoteok targets are **disabled** — Cloudflare challenges block headless CI, and both boards are already covered by the existing feed (`rss:himalayas`, `remoteok:remoteok`). The Playwright workflow has **no `continue-on-error`**; the script exits 1 only when *every* target fails, and the `jobs_fetch_reports` row is written on every path.
- **`extractJob`'s `q` contract is a SYNC element view** (`{ textContent, getAttribute }`). Playwright handles are async — the scraper adapts them via `resolveViews()`. A regression test pins this; don't "simplify" it back to raw handles.
- **Takedown engine (D3) design facts:**
  - `question_hash = md5(lower(trim(question_text)))` — Postgres-side md5; JS-side `questionHash()` in `takedown-lib.js` matches it (RFC 1321 vector in tests).
  - `buildUpsertSql(rows, suppressions?)` — empty suppressions → byte-identical SQL to pre-D3. The cron applies `takedown_suppressions` best-effort.
  - Soft takedown = one insert into `takedowns` + one status update; the DB trigger maintains suppressions and `taken_down_at`. `publishedFor()` excludes `status='taken_down'` everywhere QA flows. Pre-migration fallback: unpublish only.
  - **RAG needs no propagation:** the corpus comes from static `content/rag-seed/*.md` (`gen-rag-corpus.mjs`), not scraped questions. Do not touch `embed()`/`embedQuery()` or the `"text-embedding-3-small"` literal in `rag-eval.test.ts`.
- **Migrations applied on the live project so far:** `content-sourcing.sql` (A), `scraper-runs.sql` (B, incl. admin-insert policy), `jobs.meta jsonb` (C), `discovery.sql` (D3+D2), `discovery-public-read.sql` (D4).
- **CORRECTION (2026-09-24): D2's tables were NOT live before today.** The earlier "discovery.sql already applied incl. D2 tables" note was wrong — a live catalog query showed only the D3 tables (`takedowns`, `takedown_suppressions`, `takedown_summary`). `supabase/discovery.sql` was re-run (idempotent) after merging #75; `discovery_seeds` + `discovered_resources` + indexes + policies verified live via catalog. Lesson: verify migrations with an information_schema query, never from session memory.
- **D4 live facts (2026-09-24):** `discovery-public-read.sql` creates the read-only PUBLIC view `discovered_resources_public` over `status='approved'` rows (verified: approved row visible, pending row hidden, cleanup complete). Admin base tables stay RLS admin-only; no write path exists on the view. Admin → Content → 🔎 Discovery = Discover-from-URL (classify preview runs the crawler's own `classifySeed`+`planDiscovery`), seed/resource approval queues, credits summary. Credits plumbing: `PublishedQuestion.meta.attribution`/`source_url` → `attributionLabel()` → "via owner/repo ↗" chips in ReviewInbox + Public Bank; public `#/sources` page + footer link; Counselor "Community discovered" strip.
- **D5 facts:** gap rule = fewer than `GAP_MIN=2` approved resources attributable to a skill (counts join `discovered_resources` → `discovery_seeds.skill` — attribution hosts never match skill names, the seed column is the join). Proposals are deterministic + keyless (GitHub repo-search + HN Algolia), deduped against ALL known seed URLs (any status — rejected URLs are never re-proposed), budget-capped (20/run), and queued as `pending` `discovery_seeds` with `origin='skill-auto'`. Zero-result search proposals are dropped (fail-OPEN on probe errors — uncertainty never drops; the human gate is the fail-closed layer). `--dry-run` is a pure local preview (no DB reads, no probes). Catalog names are parsed from `skillCatalog.ts` SOURCE TEXT (regex on `name: "…"` literals) — zero eval, immune to type churn. Workflow: Mondays 06:00 UTC, secret-guarded, optional GITHUB_TOKEN.
- **deno-gate clarification (closes the "8 pre-existing failures"):** those failures were an INVOCATION ARTIFACT — `deno task test` (no deno.json exists) falls back to package.json and runs vitest under deno, which breaks jsdom tests. The actual CI gate (`deno test supabase/functions/_shared/`) is green on main: 72 passed / 0 failed. Nothing to fix; don't run vitest under deno.
- **crawl-sources dry-run contract:** `--seed`/`--dry-run` still requires SUPABASE_ACCESS_TOKEN/SUPABASE_PROJECT_REF env (validated eagerly) — dummy values suffice since no writes happen. Verified: `node scripts/crawl-sources.js --seed https://github.com/topics/interview-questions --max-total 8 --dry-run` → 8 pages, 8 problem candidates, 260 resources, 0 errors, exit 0.
- **LIVE end-to-end round (2026-09-24) — the whole loop works:** discover-weekly queued 17 pending seeds (after #78 fix) → 2 JavaScript seeds approved via SQL → crawl-weekly dispatched → **15 pages, 28 Q&A drafts** (provenance `discovery:6`/`discovery:7`, awaiting inbox review), 114 resources, 0 errors, scraper_runs row `ok`. Lesson learned the hard way: **GitHub search pages are JS-rendered** — the crawler sees nav chrome ("Login", "Copilot", `features/*`, `solutions/*`), not results; all 114 rows were adjudicated rejected with reasons (real repo links correctly flowed to Q&A drafts instead). Rule of thumb: approve `html` seeds on content pages (topic pages, blogs, repos), not search-result pages.
- **D5 live-run bug (fixed #78):** the hand-built insert had 5 expressions for 6 target columns (status omitted) — every insert failed and proposals detoured to `content/discovery-seeds.auto.json`. SQL tuples are now built in the pure lib (`buildSeedValuesSql`) with a column-count regression test. Corollary: **every live run is a test** — read the workflow logs after dispatch, don't trust a green checkmark alone (the run was "successful" while every insert failed).
- **Execution levers added:** `crawl-weekly.yml` (#79, dispatch-only — the crawler previously had NO CI trigger; schedule intentionally omitted, one cron line enables it) and `revalidate-weekly.yml` (#80, Mondays 07:00 UTC + dispatch). Both registered in `ghActions.WORKFLOWS` (admin Cron log) with tests.
- **L5 re-validation (resource-safety-guard §3):** approved resources are probed weekly (HEAD, GET fallback); unreachable/non-2xx/redirected/erroring rows auto-quarantine back to `pending` (hidden from the public view instantly, evidence in `meta.decision_note`, reported into scraper_runs). Trailing-slash normalization ≠ redirect (live-verified false positive, `sameUrl` in `revalidate-lib.js`). Content-marker drift is scaffolded (`storedMarker`) pending the guard's L3 server-side fetch.
- **Draft quality + skill tags (#81, 2026-09-24):** `scripts/draft-quality-lib.js` — `noiseReason()` (hard noise: url-as-question, playground-link, tracking-params, json-fragment, non-english CJK+Cyrillic, too-short, shout), `looksTruncated()` (review-first tier), `deriveSkills()` (canonical tags: React/Java/JavaScript/Node.js/PostgreSQL/… with alias normalization; word boundaries disambiguate Java≠JavaScript; unknown tech never guessed). `normalizeItem` derives skills centrally (cron + discovery + Run-now); `buildUpsertSql` writes the `skills` column; `routeItems` returns `noise[]` (crawler counts `noiseDropped` per seed); ReviewInbox shows 🛠 chips. **Live cleanup:** the 28 discovery drafts → 20 deleted (16 codepen URLs, 1 Cyrillic, 3 UTM/JSON), 8 keepers tagged (Python/Angular/JavaScript). Word-boundary lesson: `\bjava\b` already can't match inside "javascript" — a `not: /java\s?script/` guard wrongly killed Java in mixed texts; use lookahead `(?!\s?script)` when needed.
- **#82 follow-ups (2026-09-24):** Bank skill chips are **multi-select AND** (`bankItems(fieldSel, q, skill, skills[])` + `matchesAllSkills`; legacy single-string arg kept for drill.ts). Triage worker fail-closes truncated titles to `review-first` via `looksTruncated`. **Skills backfill:** all 33 published rows were untagged; `deriveSkills` tagged 6 (4× JavaScript, Java+C++, React), 27 generic rows intentionally left untagged (text fallback covers them) — 1 stray excluded (CSS from the word "styling" in a refactoring answer). **Editorial pass:** all 8 discovery drafts were mis-tagged `field_id='JavaScript'` (non-existent field — they could never surface); 3 republished with AI-original answers + corrected field/level (#3306 Angular binding → frontend, #3307 data structures → backend, #3308 security interview prep → security), 5 forum/repo-noise rows deleted. Bank now 36 published / 527 drafts. Discovery discovery:6/7 queue is fully drained; cron/scraper no longer auto-publishes, so the ReviewInbox stays the only publish path.
- **AI-clean key fix + second editorial push (2026-09-26):** owner pasted a getunikey.ai key (Admin → Secrets → AI pipeline). Gateway triage, all live-verified: stored model `z-ai/glm-5.2` dead upstream (524s), `tencent/hy3-free` no channel (503), `gpt-6-astra` answered beautifully but costs **200.1 credits/call vs a 174 balance** → gateway 403s whose body decodes 预扣费额度失败 (pre-deduction failed); every listed model probed, `deepseek/deepseek-v4-flash` (+gemini-3.5-flash, MiniMax-M2.7, kimi-k2.7-code) affordable and fast. **Gotchas:** the 403s are quota, not rate — cooling down never helps; and once a burst starts failing, STOP instead of re-marking (a second fail-fast sweep burned 0 rows but the first chunk of rejections predated the model switch). 286 drafts cleaned via `ai-clean-exec.mjs` (service-key PostgREST executor mirroring `ai-clean.js`'s contract: same `ai-clean-lib` prompt/parse/apply, `ai_cleaned`/`ai_cleaned_at`/`ai_failed` marks, md5 question-hash dupe guard, per-row PATCH). Editorial pass via `editorial-clean-push.mjs` (dry-run default): **281 ready / 0 dupes / 0 noise / 5 review-first** (3 terse problem titles with real answers — the known `too-short` gotcha — + 2 truncated-looking). **Publish lesson re-learned: PATCH exactly what the proven path writes — adding `status:"published"` tripped a table check-constraint (23514) 281/281; reverting to `published:true` + `skills` alone went 281/281.** Bank **269 → 550**, tag coverage **382/550 (69%)** (top: JavaScript 122, Node.js 71, System Design 71, ML 53, React 43, Design Patterns 41); difficulty + company tags carried in `meta`. Pile now 12 (classifier flags + transients) awaiting inbox review. Client pickup is automatic (boot-time full re-pull via anon key).
- **#90 pdfjs-dist 6.2.108 → 6.3.289 (2026-09-25):** routine minor bump; pdf/resume suites pass unchanged; all five gates green.
- **#89 opt-in Playwright rendering for the crawl (2026-09-25):** `scripts/discovery-render-fetcher.mjs` plugs a headless-Chromium fetcher into the D1 seam with the same `fetchText()` contract; `resolveDiscoveryFetcher` returns `{fetcher, mode, close}` — plain fetch stays the default (with a fallback note), `DISCOVERY_RENDER=1` opts in and makes crawl-weekly install Playwright (conditional step + env threading). Gotcha kept in code: the `playwright` specifier is computed at runtime (`["play","wright"].join("")` + `/* @vite-ignore */`) so vite/vitest never try to statically resolve it.
- **#88 selector-drift alarm + L5 content markers (2026-09-25):** (a) `scrape-jobs-playwright.mjs` now **exits 1** with a `SELECTOR DRIFT SUSPECTED` banner when every target loaded but zero postings matched (the old path shipped green while matching nothing); WWR selectors re-verified live intact (the category URL 302s to remote-full-stack-programming-jobs), YC confirmed still fully JS-rendered; verification notes stamped into `content/job-playwright-targets.json`. (b) L5 markers end-to-end: `markerFromText()` (revalidate-lib.js) fingerprints the first ~200 chars of visible text (script/style stripped, whitespace collapsed); `decideResource(resourceUrl)` stores `meta.contentMarker` at approval (best-effort fetch, never overwrites); the weekly revalidate selects `meta->>'contentMarker'`, GETs the body, and quarantines ONLY when both markers exist and differ — missing evidence passes on reachability (fail-closed on evidence, fail-open on unknowns).
- **#87 keyless GitHub REST search (2026-09-25):** the live round's biggest lesson — GitHub search pages are JS-rendered — is fixed at the classifier level: `classifySeed` recognizes `github.com/search?q=…&type=repositories` as a `github-search` kind; `githubSearchUrl`/`parseRepoSearchHit` (discover-lib.js) hit the keyless REST API and yield structured repos (owner/repo, html_url, SPDX license or null, stars; junk → null); `runGithubSearch` (crawl-sources.js) fans each hit into a license-stamped `github-repo` child seed the normal crawl path executes; optional `GITHUB_TOKEN` raises the rate budget; 403/429 → per-seed note, never a crash. `SeedKind` extended + 🔎 icon in DiscoverySection. Live smoke: `svelte query` → 3 repos (MIT / Apache-2.0 / none).
- **#83 draft-pile audit + topic shelves (2026-09-24):** ran the repo's own classifiers over all 527 drafts (`scripts/audit-drafts.mjs`): 24 hard noise / 39 truncated / 464 salvageable (237 with answers). Sweep deleted **58**: 48 answerless bare titles (pre-#81 nightly-crawl remnants — "Two Sum"-style titles, no answer/provenance/field; `too-short` correctly catches them but they had leaked in before the filter), 9 Tell/Show HN promos, 1 URL. **Classifier gotcha: `too-short` fires on legitimate terse problem titles** — when auditing, content (answer presence + provenance) is the delete signal, not the classifier alone. Kept: 5 thin-but-answered, all Ask-HN prep questions. `noiseReason` now returns `"hn-story"` for Tell/Show HN (announcements never publishable); **Ask HN stays review-tier** (real prep questions minus the prefix; human gate = fail-closed). `deriveSkills` gained 5 topic shelves from the static bank's untagged clusters — System Design (`^design a` anchor + `system design`; "How would you design a rate limiter?" deliberately NOT matched), Design Patterns (Law of Demeter/IoC/cohesion/coupling/Active Record/…), SOLID (phrase + case-sensitive bare acronym — lowercase "a solid understanding" must never match), TDD, Functional Programming (pure functions). 17 published rows re-tagged (2 incidental-mention strays excluded: 121 CSS-from-"styling", 3308 SQL-from-"SQL injection"); **tag coverage 7/36 → 24/36**; draft pile 527 → 469. `CANONICAL_SKILLS` has no code consumers (chips derive from `field.skills` ∪ item tags), so extending it is UI-free.
- **Skill chips AND→OR (2026-09-25, owner decision):** AND zeroed out cross-tag sets (AWS+CSS+System Design → "Showing 0 of 499"); owner: "Multiselect must be if any of the tag matched not all". `matchesAllSkills` → `matchesAnySkill` (`skills.some(matchesSkill)` with an explicit empty-guard — `[].some()` is false, so an empty selection would have hidden everything; the old `[].every()` was vacuously true). Applied to public `bankItems` + both admin lists; tests rewritten to pin OR semantics (one hit suffices; text fallback per-skill still applies). Gates: typecheck/build/vitest 1451/rag 41/deno 72.
- **Review Inbox skill chips for the untagged backlog (2026-09-25, follow-up):** the #84 inbox chip row only rendered when drafts carried explicit tags — all 230 backlog drafts are untagged, so the owner saw no filter at all. Fix: the triage Web Worker now emits `skills` per draft (`draftSkills` in `duplicates.ts`: explicit tags win; else `deriveSkills(question+answer)` — the same classifier the cron uses, zero extra requests, still off main thread); DraftCard renders 🛠 chips from that source; the chips row is always live when any draft yields tags; the skill filter matches against the same derived tags (so filtering React finds text-derived React drafts too). Caveat: derived chips appear as triage results stream in (~first paint may briefly show fewer chips). Test: `draftSkills` explicit-wins/derived/never-guessed in `quality.test.ts`.
- **Admin multi-select skill chips + backfill (2026-09-25, same session):** owner asked for all four follow-ups at once. (1) `FilterChip` extracted into `ui/` (Bank's local component → shared barrel; Bank.tsx imports it now). (2) Both admin filter rows upgraded from the first-pass single select to **multi-select AND chips** identical to the public Bank — `matchesAllSkills(toBankItem(row), selected)`; `toBankItem` maps PublishedQuestion → BankItem shape. (3) `deriveSkills` gained 4 conservative shelves for the published bank's untagged long tail: Machine Learning (tight phrases — overfitting, bias-variance, gradient descent…; **`regression` guarded against "regression testing"**), Data Structures (hash table/binary tree/complexity/Big-O), Microservices, Concurrency (deadlock/race condition/semaphore) — generic words ("model", "learning", "thread") never match alone. **Bug found during backfill dry-run:** the CSS alias `\bless\b` matched the English word "less" — #113/#121/#169 (goto/refactoring/conditionals) got CSS from it; alias now requires preprocessor context (`less preprocessor|less.css|Sass or Less over plain CSS`), regression test added. (4) Live backfill via service-key PostgREST (batched by tag-set, dry-run first): **91/155 untagged rows tagged** (ML 29, JS 25, React 15, Data Structures 10, FP 9, DP 5, Micro 5, …), 64 left untagged by design (no confident tag). Tag coverage **112/269 (42%) → 203/269 (75%)**; Question Bank rows already rendered 🛠 chips, so the backfill is what makes them appear (the owner screenshots showed untagged rows). Stray exclusions 121/3308 carried forward.
- **Admin skill filters, first pass (2026-09-25, owner-reported gap):** the Item-A skill filter shipped on the public Bank only — Admin → Question Bank and Review Inbox had no skill control (screenshots 2026-09-25). Added `publishedMatchesSkill` (reuses `matchesSkill` on a mapped row: explicit tag wins case-insensitively, q/a/kp text fallback for untagged) and `adminSkillChips` (row tags only, deduped case-insensitively, sorted — no static field catalog in admin lists) in `engine/bank.ts`; both surfaces get an "All skills / 🛠 …" select wired into the existing filter row (status filter unchanged), Clear + page-reset include it, Review Inbox hides the select when no draft carries tags. Gates: typecheck ✓, build ✓, vitest 1449 (+2 in `admin.test.ts`), rag 41, deno 72.
- **Editorial push (2026-09-25) — draft pile 469 → 230, bank 36 → 269.** Classifier+dupe audit over a fresh read-only snapshot (`local-read-query.mjs` reads PostgREST with the service key; `.freebuff/` is write-blocked for agent file tools — scratch executors live at repo root, gitignored). Buckets: **237 ready** (answered, `noiseReason`-clean, median answer 583 chars, 197/237 with key_points) → minus **7 dupes** (in-pile #1396/1397/1407 dupes of #142/143/1053, answerless #1484/1485 dupes of #656/657, #833 duplicating published #116 → 6 deleted incl. overlap) = **233 published** via service-key PostgREST PATCH batched by derived tag-set (145 untagged + 88 tagged: System Design 29, React 25, JavaScript 18, Design Patterns 8, FP 5, AWS 7, HTML 2, TDD 2, Java 2, C++ 2). Field/level vocab verified against `FIELDS`/`LevelId` before writing (the #82 mis-field lesson): pile holds only backend/frontend/fullstack/data + senior/mid/junior. **Left in the pile (230):** 227 answered-less drafts blocked on the AI-clean 401 (backend-mid 107, arialdo 58, hn-system-design 51…) + 5 truncated-but-answered review-first rows (#1386/1398/1399/1400/1409 — classifier fires on terse titles, content is fine; human gate). Client pickup: no cache-bump needed — `App.tsx`'s `subscribeCloud → refreshAdminData()` re-pulls the full bank through the anon key on every boot (verified anon count = 269).
- **#91 Make ATS-ready resume rewrite (2026-09-26, owner request):** one-click **🎯 Make ATS-ready** on ResumeCard opens a modal whose job picker is shortlisted by skill overlap. `atsReady.ts` rewrites the stored resume through the existing AI ladder (`chat(module:"ats")` — BYOK → cloudChat → error) with a strict-JSON contract (`isValidResume` rejects <120 chars / single-line / HTML / fenced output) and a `repairContacts` pass that re-inserts email/phone/LinkedIn the model dropped below line 3; mirrored skills are filtered to the target job's skills. The modal shows before/after **atsParsePreview** scores + a diff toggle, and "use as my resume" re-extracts via `applyResume` (blanket `probeJob` when no target job). `moduleModels` gains the `"ats"` module (suggested `gpt-4o-mini`, needsJson) so owners can route it to a cheaper model. Gates: vitest **1467** (+8 in `atsReady.test.ts`), rag 41, deno 72/0; merged c3ed4975, Pages deploy 36220910668 success.
- **#92 working-model discovery + 2-min auto-apply (2026-09-26, owner request):** after pasting the getunikey key the gateway's stored model kept dying upstream (glm-5.2 524, tencent 503, gpt-6-astra 403 quota-预扣费), so the admin Secrets page now **finds working models itself**. `ai-chat` gained two admin-gated POST actions: `probe-models` (1-token live probe, nonce `#p${Date.now().toString(36)}` cache-bypass, 30 s AbortController cap, MAX_PROBES=60, media/embedding models excluded) and `set-provider-model` (read-modify-write PATCH of the `ai_provider_config` value blob via SERVICE_ROLE_KEY — no key re-entry). `aiModelPicker.ts` ranks **probed models only** (`buildModelOptions` never suggests unprobed ones) with plain-language task descriptions (`describeTask`: embeddings/image/video/vision/code/thinking/fast/flagship/general; auto-pick names −40, non-thinking −10, opus/ultra/max +25, non-chat +200, latency bonus); `SecretsSection.ModelScanCard` opens automatically after a key save (plus a standalone 🔍 Find working models button) and **auto-applies the winner after a 120 s countdown** ("Use this" cancels; stand-down toasts on failure). Deployment gotcha fixed: **`ai-chat` was missing from deploy.yml** (hand-deployed only — a Pages deploy silently kept the stale function); now registered after seed-rag with a drift comment. Live smoke verified all three probe outcomes on the real gateway before merge. Gates: vitest **1478** (+11 in `aiModelPicker.test.ts`), rag 41, deno 72/0, deno check ai-chat ✓; merged bd732ceb, Pages deploy 36222391094 success with `ai-chat` bundled (776 kB).
- **#93 model-list CORS fix (2026-09-26, owner-reported from the deployed site):** the 🔍 scan said **"Couldn't list models"** while "Test key" (direct browser→provider) was green and chat worked. Live triangulation: provider `/models` 200/44 models from curl, deployed edge GET /models 200 with 44 models from curl — but **no `Access-Control-Allow-Origin` on the actual GET response** (preflight was fine). Root cause: `handleListModels`/`handleProbeModels`/`handleSetProviderModel` built their own bare Responses (Content-Type only), dropping the top-level prepared CORS headers; browsers block a CORS-less 200 and `fetchAvailableModels` swallowed the resulting network error into `[]`. **Why the smoke test missed it: curl never enforces CORS** — every pre-merge probe was from node/curl. Fix: handlers thread the prepared headers (13 bare headers gone), the client wrapper throws with the real reason (edge error / network-or-CORS / signed-out), and empty lists are never cached (a transient gateway hiccup previously stuck for the 5-min TTL). ConfigSection's module-model dropdown had been silently hitting the same wall. Gates: vitest **1486** (+8 in `aiModels.test.ts`), rag 41, deno 72/0; merged 49c84241, deploy success; **live-verified with browser-equivalent headers: GET 200 + ACAO + 44 models, POST error paths carry ACAO too, admin gate intact.**
- **#94 + 92891c08 scan stale-report + no-listing fixes (2026-09-26, owner-reported):** after switching the saved key to agentrouter.org the scan card kept showing the PREVIOUS provider's models. Two defects: (1) **ModelScanCard only auto-scanned idle→open** — it now tracks the saved provider identity (`keyHint@base`) and re-scans on change, plus a manual ↻ Rescan button; (2) **agentrouter serves no JSON /models** — `GET /models` returns its SPA's HTML page with 200, `/v1/*` 401s with "unauthorized client detected" (it blocks non-browser API clients entirely; UA spoofing doesn't help — the gateway is unusable for this app, owner should switch back to getunikey or use the agentrouter Discord). Discovery now survives listing-less gateways: the edge probe action merges the saved model + a common chat-family candidate set when the listing is unavailable, and an **HTML-200 probe is rejected as `non-JSON response`** instead of counted "working" (agentrouter's SPA would otherwise have poisoned the ranking). Same class of false-positive fixed in **Test key** (92891c08): a non-JSON 200 now fails with "served a web page, not the API" — it had shown green on agentrouter. Client contract: `fetchAvailableModels` throws a distinct "provider does not expose a model list" error (expected on these gateways — scan proceeds on probe verdicts alone). Gates: vitest **1489** (+3), rag 41, deno 72/0; PR #94 merged affab66a + direct fix 92891c08, deploys success. Tag coverage now 112/269 published.
- **Recommended D sub-order** (per plan): D1 → D3 → D2 → D4 → D5. **D2 built** (2026-09-23): `crawl-sources.js` crawls APPROVED seeds only (approval gate = crawl trigger), BFS within Budget, robots-checked, reuses `extractItems`/`buildUpsertSql` for drafts, classifies links via `crawl-orchestrate-lib.js` (qa / problem-candidate / resource), inserts `discovered_resources` pending, reports into `scraper_runs` (trigger 'cron'). Per-page fetch failures are counted notices (a seed errors only when nothing loads); problem candidates come from both extracted titles and problem-shaped links. Note: `deno task test` has 8 pre-existing failures on main (`fnjudge.test.ts`, `jsfunctions.test.ts` — jsdom/runner, unrelated); the gate is green **relative to main** (delta +24 passing).
- **Nightly cron is now fully healthy (2026-09-23):** first green end-to-end run inserted **330 drafts** with the oversize drop surfaced as a notice. Remaining owner action: the AI-clean step gets `AI HTTP 401` — the key stored in Admin → Secrets → AI pipeline is rejected; replace it to re-enable cleaning.
